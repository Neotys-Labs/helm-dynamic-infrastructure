{{ if .Values.registryKey.enabled -}}
- Set the image pull secret by running this command:
kubectl patch sa {{ .Release.Name }}-sa -p '{"imagePullSecrets": [{"name": "{{ .Release.Name }}-rk"}]}' -n {{ .Release.Namespace }}
{{- end }}